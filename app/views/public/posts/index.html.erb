<% @posts.each do |post| %>
  <div class = 'card'>
    <%= post.user.name %>
    <div class = 'card-title'><%= post.title %></div>
    <div class = 'card-body'><%= post.body %></div>
    <%= link_to '詳細', post_path(post.id) %>
  </div>

<% end %>